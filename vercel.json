{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/app.py" }
  ],
  "builds": [
    { "src":"/api/app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "source": "/api/(.*)", "dest": "/api/app.py" }
  ]
}
