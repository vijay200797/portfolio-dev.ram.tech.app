{
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/app" }
  ],
  "builds": [
    { "src":"/api/app.py", "use": "@vercel/python" }
  ],
  "routes": [
    { "source": "/api/(.*)", "dest": "/api/app" }
  ]
}

